add_library(utils INTERFACE)

target_include_directories(utils INTERFACE ${CMAKE_CURRENT_LIST_DIR}/../../../utils)

target_sources(utils INTERFACE
  ${CMAKE_CURRENT_LIST_DIR}/../../../utils/eval.cpp
)
